import { GoogleLinkAuthGuard } from './google-link-auth.guard';

describe('GoogleLinkAuthGuard', () => {
  it('should be defined', () => {
    expect(new GoogleLinkAuthGuard()).toBeDefined();
  });
});
